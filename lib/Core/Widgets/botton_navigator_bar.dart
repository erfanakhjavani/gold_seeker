

import 'package:flutter/cupertino.dart';

class MainNavigatorBar extends StatelessWidget {
  const MainNavigatorBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Row();
  }
}
